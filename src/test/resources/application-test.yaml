spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb?options=DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
  sql:
    init:
      schema-locations: classpath:schema.sql
      mode: always
  liquibase:
    enabled: false
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    root: WARN
    ru.iakovlysenko.wallet: DEBUG
